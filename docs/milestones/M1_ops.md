# M1 Ops

## Added
- Local infra definition (`docker-compose.yml`) for PostgreSQL and Redis.
- Basic run commands in `Makefile` and `README_BACKEND.md`.

## Missing for Next Milestones
- Production deployment manifests.
- Alert rules and dashboards.
- Backup/restore runbooks.
